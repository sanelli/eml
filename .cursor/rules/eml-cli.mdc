---
description: eml CLI commands, flags, file extensions, and variable bindings
alwaysApply: true
---

# Command line

The Ada executable is invoked as `eml`. Front-end is chosen from the **input extension**: `.teml` (source expression) or `.eml` (EML text).

## Global options

- `--no-color` — plain-text diagnostics and usage on stderr (no ANSI color). Allowed before or after the subcommand.
- `--no-logo` — suppress the startup banner. Allowed before or after the subcommand.

## Shared preprocessor options

On `preproc`, `tokenize`, and `parse`:

- `--var` / `-v $VARNAME=EXPRESSION` — repeatable; `$` required; split on first `=`; empty EXPRESSION is invalid CLI; repeated name is invalid CLI
- `--warn` / `-w default|none|error` — default is `default` when omitted
  - `default` — print unused `--var` warnings (yellow stderr); do not stop
  - `none` — suppress unused `--var` warnings
  - `error` — treat unused `--var` as error (red stderr); stop before next stage

## `help`

```
eml help
eml help preproc
eml help tokenize
eml help parse
```

- `eml help` — usage, command list, and common options on stdout; exit `0`
- `eml help preproc` — preproc-specific usage on stdout; exit `0`
- `eml help tokenize` — tokenize-specific usage on stdout; exit `0`
- `eml help parse` — parse-specific usage on stdout; exit `0`
- Unknown topic → stderr error + usage, exit `1`
- Banner is still printed unless `--no-logo` is set

## Startup banner

Unless `--no-logo` is set, `eml` prints a one-line banner to **stdout** (never stderr, never the `--output` file):

```
EML compiler and interpreter - v0.1.<short-hash> - by Stefano Anelli
```

`<short-hash>` is the embedded git commit; `<major>.<minor>` comes from the crate version (e.g. `0.1.0-dev` → `0.1`).

With `--output`/`-o`, the banner still goes to stdout while command output goes only to the file. Without `-o`, stdout is the banner (unless `--no-logo`) then the dump; use `--no-logo` for a pure dump (pipes, tests).

## `preproc`

Substitute `$VARNAME` from `--var` bindings and write expanded `.teml` text.

```
eml preproc --input|-i <file.teml> [--output|-o <file.teml>] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
```

- `--input` / `-i` — required; must end in `.teml`
- `--output` / `-o` — optional; if present must end in `.teml`; if omitted, output goes to stdout
- Unbound `$VARNAME` — error for **every** occurrence; no output; exit `1`
- Unused `--var` — warning (or error with `--warn error`)
- No `$VAR` in file — output equals input (after `Read_File` newline normalization)

Examples:

```
eml preproc -i filename.teml
eml preproc -i filename.teml -o other.teml
eml preproc -i f.teml -v '$X=1+2'
eml --no-logo preproc -i f.teml -v '$X=1' -w none
```

## `tokenize`

Run preprocessor, then dump the token stream. Input is required; output is optional.

```
eml tokenize --input|-i <file.teml> [--output|-o <file.tokens>] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
```

- `--input` / `-i` — required; must end in `.teml` (this stage; `.eml` tokenize comes later)
- `--output` / `-o` — optional; if present must end in `.tokens`; if omitted, the dump goes to stdout
- Flag order after `tokenize` is free; space-separated only (no `--input=`)

### `.tokens` dump format

One token per line, no header, no EOF token, no skipped whitespace:

```
<line>:<column> <KIND> <lexeme>
```

`line` and `column` are 1-based **original** source positions. Around each run
of tokens from the same substitution occurrence, the dump inserts comment lines
(not tokens):

```
-- $VAR1 begin
1:8 NUMBER 1
-- $VAR1 end
```

Invalid tokens are not written to the dump.

### Errors and exit codes

- Invalid CLI → short error + usage on stderr (red unless `--no-color`), plus a hint to try `eml help`, exit `1`
- Missing command → `error: missing command (expected help, preproc, tokenize, or parse); got '…'` (or without `; got …` when no args), exit `1`
- Preprocess unbound `$VAR` → `error: undefined variable $X at line N, column M` (all occurrences); no dump; exit `1`
- Unused `--var` → `warning: unused variable $Y` (yellow) or `error: unused variable $Y` with `--warn error`; exit `1` only in error mode
- Lex errors → `error: invalid token at line N, column M: …` optionally `(from $VAR)`; continue scanning; still emit valid tokens; exit `1`
- I/O failure → stderr diagnostic, exit `1`
- Success → exit `0`

Usage text (errors):

```
Usage:
  eml <command> [options]
  eml help [command]
  eml preproc --input|-i <file.teml> [--output|-o <file.teml>] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
  eml tokenize --input|-i <file.teml> [--output|-o <file.tokens>] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
  eml parse --input|-i <file.teml> [--output|-o <file>] [--output-format|-of mermaid|md|dot|svg] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
Try 'eml help' for more information.
```

Examples:

```
eml tokenize --input filename.teml --output other.tokens
eml tokenize -i filename.teml -v '$X=1'
eml tokenize --no-logo -i filename.teml -o other.tokens
```

`.eml` tokenize is not implemented yet; treating a `.eml` input as tokenize is invalid CLI for now.

## `parse`

Preprocess a `.teml` file, tokenize, then build a syntax tree with standard mathematical operator precedence and parentheses. Dump the tree.

```
eml parse --input|-i <file.teml> [--output|-o <file>] [--output-format|-of mermaid|md|dot|svg] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
```

- `--input` / `-i` — required; must end in `.teml` (this stage; `.eml` parse comes later)
- `--output` / `-o` — optional; if omitted, the dump goes to stdout
- `--output-format` / `-of` — optional; default `mermaid`. Values: `mermaid`, `md`, `dot`, `svg`
- Flag order after `parse` is free; space-separated only (no `--input=` / `--output-format=`)
- Parse `-of` / `--output-format` as their own flags so `-of` is never treated as `-o`

### Output format vs extension

When `-o` is present, the extension **must match** the selected format:

| `-of` | `-o` extension | Contents |
|---|---|---|
| `mermaid` (default) | `.syntaxtree` | Raw Mermaid `flowchart TD` |
| `md` | `.md` | Markdown with a fenced Mermaid block |
| `dot` | `.dot` | Graphviz `digraph` |
| `svg` | `.svg` | Self-drawn SVG (no Graphviz) |

Mismatch is invalid CLI. Without `-o`, the same format is written to stdout.

Pipeline: read file → preprocess → if unbound or `--warn error` unused, report and stop → tokenize → if lex errors, report and stop (no tree) → parse → if parse error, report and stop (no tree) → emit.

Parse errors stop at the first error:

```
error: parse error at line <line>, column <column>: <reason>
```

Optionally append `(from $VARNAME)` when the error location comes from substituted text.

Exit `0` on success; `1` on invalid CLI, I/O, preprocess, lex, or parse errors.

`eml help parse` — parse-specific usage on stdout; exit `0`.

`.eml` parse is not implemented yet; treating a `.eml` input as parse is invalid CLI for now.

Examples:

```
eml parse -i filename.teml
eml parse -i filename.teml -o other.syntaxtree
eml parse -i filename.teml -o other.md -of md
eml parse -i f.teml -v '$X=1' -w none
eml parse --no-logo -i filename.teml -of svg -o other.svg
```

## `compile`

Lower a `.teml` syntax tree to stack-machine IR and write textual `.eml` or binary `.beml`.

```
eml compile --input|-i <file.teml> [--output|-o <file>] [--format|-f eml|beml] [--var|-v $NAME=EXPR]... [--warn|-w default|none|error] [--no-color] [--no-logo]
```

- `-i` / `--input` — required; must end in `.teml`
- `-o` / `--output` — optional; extension must match `--format` (default `beml` → `.beml`); omit → stdout
- `--format` / `-f` — optional; default `beml`; values `eml` or `beml`. Valid on **`compile` only** (`--format` on preproc/tokenize/parse is invalid CLI). `-of` / `--output-format` stay parse-only and are invalid on `compile`.
- `--var` / `--warn` / `--no-color` / `--no-logo` — same as parse
- Flag order free; space-separated only (no `--format=`)
- Repeated `--format` is invalid CLI
- `eml help compile` → usage on stdout, exit `0`
- Unknown `--format` values (including future `cli`, `bytecode`, `binary`) are invalid CLI
- Exit `0` on success; `1` on invalid CLI, I/O, preprocess, lex, or parse errors

Output extension must match format:

| `-f` | `-o` extension |
|---|---|
| `beml` (default) | `.beml` |
| `eml` | `.eml` |

Pipeline: read file → preprocess → tokenize → parse → lower AST to IR tree → encode as `eml` or `beml`. Stop with no output file on unbound/`--warn error`, lex, or parse errors (same gates as parse).

Examples:

```
eml compile -i filename.teml
eml compile -i filename.teml -o other.beml
eml compile -i filename.teml -o other.eml -f eml
eml compile --no-logo -i f.teml -f eml
eml compile -i f.teml -v '$X=1' -w none -o out.beml
```

Native / .NET / JVM backends (`--format cli`, `bytecode`, `binary`) are not implemented yet.

## `run`

Interpret. Bind variables as extra arguments `$NAME:VALUE`. `VALUE` is a source constant (decimal, scientific, `i`, `e`, `pi`, `phi`, …).

```
eml run --input filename.teml $VAR1:1.345 $VAR2:i $VAR3:e $VAR4:99
eml run --input filename.eml $VAR1:1.345 $VAR2:i $VAR3:e $VAR4:99
```

Do not invent extra subcommands or flags beyond this surface unless the user asks.
